
import { Button } from '@/components/ui/button';

export default function PetButton() {
  return (
    <Button className='w-8 h-8 hover:scale-150 hover:text-lg rounded-full absolute bottom-10
    right-6 opacity-50 hover:opacity-100 transition-all'>
      +
    </Button>
  );
}